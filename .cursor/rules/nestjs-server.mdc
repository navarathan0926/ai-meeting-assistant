---
description: NestJS server conventions — controllers, services, modules
globs: server/**/*.{ts,tsx}
alwaysApply: false
---

# NestJS Server Conventions

Applies to `server/`.

## Feature module checklist

Each feature folder under `server/src/` should include:

- `*.module.ts` — registers controller, service, and imports
- `*.controller.ts` — HTTP routes only
- `*.service.ts` — business logic
- `dto/` — request validation classes
- `entities/` — TypeORM entities (when persisted)
- `interfaces/` — response/API shapes (optional, when not covered by DTOs)
- `enums/` — string enums for repeated domain literals (roles, statuses, error codes)

Infrastructure modules (`database/`, `storage/`, `common/`) follow the same naming pattern but may omit controllers or entities.

## Enums and constants

- If a string/number literal is used in **two or more** places (or is part of the public API), define a **string enum** under `enums/` and import it.
- Use enums in entities, DTOs (`@IsEnum(...)`), services, guards, and tests — never compare against raw `'USER'` / `'ADMIN'` strings.
- One-off UI copy or log messages do not need enums; domain state and protocol values do.
- Keep client enums in sync when the value is returned to or accepted from the API.

## Controller rules

```typescript
// ✅ GOOD — delegate to service
@Post()
create(@Body() dto: CreateMeetingDto) {
  return this.meetingsService.create(dto);
}

// ❌ BAD — business logic in controller
@Post()
create(@Body() dto: CreateMeetingDto) {
  const meeting = this.repo.create(dto);
  return this.repo.save(meeting);
}
```

## Common code

Place guards, pipes, filters, interceptors, middleware, and decorators in `server/src/common/`, not inside feature modules unless feature-specific.

## Service rules

- One primary responsibility per service.
- Split when handling unrelated domains (e.g. `MeetingsService` vs `BlobStorageService` in `storage/`).

## Security (server)

- Authorize from JWT/user context; scope org data by `organizationId` from the authenticated user.
- Use `UserRole` (and other domain enums) in guards and services — never raw role/status strings.
- Never return password hashes, refresh secrets, or third-party tokens in API responses.
